'use strict'

// Part 3.1

/* let min = (a, b) => {
	console.log(a > b ? b : a);
} */

//Part 3.2

/* let isEven = number => {
	if (number == 1) {
		return false
	} else if (number == 0) {
		return true
	} else {
		return number > 0 ? isEven(number - 2) : isEven(-number)
	}
} */

//Part 3.3

/* let countBs = str => {
	return countChar(str, 'B')
}

let countChar = (str, x) => {
	let k = 0
	for (let i = 0; i < str.length; i++) {
		if (str[i] == x.toString()) k++
	}
	return k
}
 */



